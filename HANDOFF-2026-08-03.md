# HANDOFF — 2026-08-03

> Sesión de **endurecimiento de caja + corrección de reportes de propinas**, con **dos pases a PROD
> firmados** por el dueño. Todo lo tocado en prod es **plata-adyacente pero sin cambio de esquema ni
> db write**; sagrados byte-idénticos en todo momento. Cierre del día: **staging reconciliado** con el
> fix `covered_role` (que ya estaba en prod) y docs al día.

## Estado al cerrar

| | |
|---|---|
| `origin/main` | **`723f734`** — merge `--no-ff` del pase `covered_role` en reportes (parent1 `b36a382`). **Deploy GitHub Pages ✅** verificado por contenido (`version.json`=`723f734`; chunk `TipStats-*` contiene `covered_role`, ausente en el viejo). |
| `origin/staging` | **`7d768d1`** — FF-merge de `feat/reporte-propina-electronica` (reconciliado con prod: ahora trae `covered_role` + `monthly-report/pool.ts`). |
| Sagrados | `tipCalculations` **`7603ba5a`** · `cashUtils` **`b597c697`** — **intactos** todo el día (`posFiscal` no existe en main). |
| Edge Function (prod) | `monthly-report` **redeployada** (`--project-ref yiczgdti… --use-api`; el link CLI **nunca salió de staging**). **Correo de Julio 2026 ENVIADO**: `ok:true`, `propinas: ₡2.167.131 · 61 turnos`. |
| Gates (cada pase) | build prod EXIT 0 ✅ · suite verde (prod 458 / staging 538) ✅ · **oracle 8/8** ✅ · ESLint lint-neutral ✅ |

## Qué se hizo

### 1) PROD ← ítems 6 + 4 del endurecimiento de caja — merge `b36a382`
Cherry-pick selectivo desde staging sobre rama desde main (nunca merge staging→main):
- **Ítem 6 — "Ingreso adicional" endurecido** (`fbb1018`): categoría (dropdown) + motivo obligatorios;
  umbral ₡100.000 (equiv. CRC) exige autorización de gerencia. subcategory sigue `'Ingreso adicional'`;
  categoría+motivo van en `description`. **Sin esquema.**
- **Ítem 4 — guard de fechas de caja** (`cfdb648`): apertura fija en hoy (`min=max` + revalida en
  `handleApertura`); "Nuevo movimiento" con backdateo solo owner/manager/contador (gate por rol) +
  confirm reforzado si el día ya tiene cierre `'completo'`. **Enforcement por rol en cliente, sin RPC.**
- **⚠️ Resolución quirúrgica del choque cosmético:** el cherry-pick de Ítem 6 **CHOCA** en
  `AgregarAsistente.tsx` porque `fbb1018` nace sobre el cosmético `6b7a7bc` ("la dueña"→"el dueño") que
  **prod no tiene**, y el código nuevo quedó pegado a esos 2 comentarios. Se resolvió tomando `--theirs`
  (todo Ítem 6) y revirtiendo a mano los 2 comentarios a "la dueña" de prod. Verificado: `diff <rama>
  fbb1018 -- AgregarAsistente.tsx` = **solo esas 2 líneas** → lógica de plata byte-idéntica al commit revisado.
- Diff `2e6fba1..b36a382` = 7 archivos, cero PoS/sagrados. Deploy Pages verificado por chunk.

### 2) PROD ← `covered_role` canónico en reportes de propinas — merge `723f734`
- **Diagnóstico:** el **correo mensual** perdía la `propinaSala` (las propinas INDIVIDUALES de sala/datáfono,
  `tip_entries.tip_amount_crc/usd`) — sumaba solo las columnas de la sesión (efectivo+barra) → Julio daba
  **₡471.311** en vez de **₡2.167.131**. Y la clasificación sala/barra correcta es por **rol EFECTIVO =
  `covered_role ?? rol base`** (lo que la persona CUBRIÓ ese turno). **Quincenal (TipHistory) YA lo hacía
  bien**; **Estadísticas (TipStats) OMITÍA `covered_role`** (usaba rol base → ₡2.160.860, sutilmente mal).
- **Fix (4 archivos):** `monthly-report/pool.ts` (`poolTotalSesion`, role-agnóstico) replica
  `calcTurno`/`calcHistory.totalPool`; `monthly-report/index.ts` trae `tip_amount_*`+`covered_role` y
  clasifica por effectiveRole; `TipStats.tsx` pasa `covered_role` a `calcHistory`. **Oracle test**
  (`pool.test.ts`, `calcHistory` como oráculo, incl. caso `covered` cruzando sala↔barra) ata la
  reimplementación al peso — Deno no puede importar el sagrado.
- **Invariante de conservación (dueño):** por sesión, `pool(covered) == Σ payout` de TODOS los empleados
  dentro de **±₡1** → **las 61 sesiones de Julio cierran** (`max_dif=1`). Las sesiones **07-18 AM** y
  **07-21 AM** (que "descuadraban" ₡3.270 y ₡2.999 bajo rol base) reconcilian bajo `covered_role` → eran
  **artefactos de clasificación**, no descuadres.
- **Los tres reportes = ₡2.167.131** (÷61 = ₡35.424... *nota: ₡35.527*): Estadísticas · Quincenal · Correo.
- **Sagrado `tipCalculations` NO tocado** — a `calcHistory` solo se le PASA `covered_role`.

### 3) Cierre — staging reconciliado
`feat/reporte-propina-electronica` (2 commits: `ba1a287` fix del correo + `7d768d1` covered_role) **FF-merge
a staging** → **`7d768d1`**. Staging estaba 2 commits atrás de prod en este fix; ahora al día.

## Divergencia ESPERADA (no drift)
`AgregarAsistente.tsx` difiere main↔staging por **2 comentarios** ("la dueña" en prod vs "el dueño" en
staging): el **cosmético `6b7a7bc` (ítem 8) está en staging pero DIFERIDO en prod**. Es divergencia
legítima, no deuda. Cuando el ítem 8 pase a prod, converge.

## Qué queda (backlog)

1. **🖊️ Bloque plata — ítems 2 + 3** (`feat/plata-negativos-ajustetipo` **`930ad83`**, **validado por el
   asesor**): (2) prohibir montos negativos en la CARGA MANUAL (asistente / nuevo mov / inline / draft de
   pago), (3) `ajuste_tipo` derivado del signo de la diferencia. **Falta: rebasar sobre el nuevo staging
   (`7d768d1`) + pase a prod firmado.** Toca plata → firma.
2. **🖊️ Ítem 1 — dirección obligatoria en traspasos (1a + 1b):** exigir origen→destino en el alta; resolver
   los `'Otro traspaso'` existentes. Toca plata → firma.
3. **🖊️ Ítem 5 — auditoría de ediciones** de movimientos (hoy solo hay auditoría de borrados vía
   `movement_deletions`; la edición pasa por reemplazo y no deja el mismo rastro). Puede tocar esquema.
4. **🧹 Ítem 7 — 2 huérfanos** de fecha imposible (`9b79e731` 2020-07-09 ₡74.126,92 y uno de 2016 ₡54.978).
   La app no los muestra (`getAllCashMovements(days=1000)`). Pendientes que nadie va a pagar.
5. **🎨 Ítem 8 → prod:** el cosmético "la dueña"→"el dueño" (`6b7a7bc`, ya en staging) — pase a prod (haría
   converger `AgregarAsistente.tsx` main↔staging).
6. **👁️ Mejora UI (letra chica):** en Estadísticas, mostrar "generado por cobertura" cuando alguien cubrió
   otro puesto (para explicar por qué su clasificación de pool cambió ese turno).

## Sagrados / guardrails respetados
`tipCalculations` `7603ba5a` · `cashUtils` `b597c697` byte-idénticos en todo momento. Cero PoS a prod, cero
esquema, cero db write. Prod solo se tocó con firma del dueño. El link del CLI de Supabase **nunca salió de
staging** (deploy de la función por `--project-ref`).
