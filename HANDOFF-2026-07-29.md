# HANDOFF — 2026-07-29

> Sesión de **feature/fix del Reporte de Ventas** (el reporte imprimible del owner, `ventas/ReporteMensual.tsx`).
> **Cuatro cambios** — todos en **un solo archivo**, display/analítica, **cero esquema, cero plata**. Sagrados
> intactos. Construido en staging por pasos, validado en piso por el dueño, y **pasado a PROD con firma** —
> ✅ **live y verificado por contenido**. Continúa el handoff del 2026-07-28 (Etapa 2 vista del empleado, ya en prod).

## Estado al cerrar

| | |
|---|---|
| `origin/main` | **`1748205`** — merge `--no-ff` del paquete del reporte (4 picks) sobre `73682ab` (Etapa 2). Deploy GitHub Pages ✅ verificado por contenido (`version.json`=`1748205`; chunk `ReporteMensual-B8WlniGR.js` con `document.body`/`primeros`/`Proyección`/`completado`; ausentes `alcanzado` y `#faf9f5`). |
| `origin/staging` | **`a6ce9e2`** — mismos 4 commits (fuente de la validación) |
| Scope del pase | **1 archivo** (`ReporteMensual.tsx`, +49/−8), **cero PoS**, byte-idéntico main↔staging |
| Sagrados | `tipCalculations` `7603ba5a` · `cashUtils` `b597c697` — **intactos** |
| Gates (rama del pase + merge) | `build` EXIT 0 · **424 tests** (baseline de main) · ESLint limpio · sagrados byte-idénticos |
| Rollback | `git revert -m 1 1748205 && git push origin main` — sin esquema |

## Qué se hizo (paquete del Reporte de Ventas)

| # | Cambio | Commit staging → pick en prod |
|---|---|---|
| **1** | **Fix PDF en blanco.** El overlay del reporte se montaba **inline en `#root`**; el `@media print { body > * {display:none} }` escondía a `#root` (ancestro) y `.rpt-overlay {display:block}` no podía revertirlo → PDF vacío. Fix: **`createPortal(overlay, document.body)`** → el overlay es hijo directo de `<body>`. | `abb929c` → `e343e64` |
| **2** | **Comparaciones justas.** "vs mes anterior" ahora compara **misma ventana de días** (N = mayor día-del-mes con datos; primeros N días vs primeros N días), no el mes completo. **Nuevo "vs mismo mes año anterior" (YoY)**, misma ventana. Null-safe (`—`). Datos del año anterior sí llegan (`getVentasHist` cubre 2023–2025). | `e64f059` → `7375908` |
| **3** | **Bloque de Proyección de meta** en el reporte (antes solo en la barra del dashboard): % completado · ₡ actual de ₡ meta · estado + Proyección ₡ (%) · Meta diaria implícita. **Reusa `metaProgress`** → coincide al colón con el dashboard. | `2e3a518` → `71e1904` |
| **4** | **Estilo plano** del bloque de proyección: sin recuadro/fondo, sin número grande; conserva **verde (sobre meta) / rojo (por debajo)** solo en la línea de proyección. | `a6ce9e2` → `2abff1a` |

**Pase a prod (2026-07-29):** port **selectivo** (cherry-pick de los 4, NO merge de staging → cero PoS). Base de
`ReporteMensual.tsx` idéntica main↔staging → cherry-pick limpio, sin conflictos. Merge `--no-ff` `1748205`.
Firma del dueño: **"pasar a prod"**.

> 🧭 **Nota para verificar deploys del reporte:** el `ReporteMensual-*` que aparece en el preload del entry es el
> **hermano** `resumen/ReporteMensual`. El reporte de Ventas que tocamos es **`ReporteMensual-B8WlniGR.js`**
> (lazy-import de `VentasContabilidad`). Caminar hasta ese chunk para confirmar contenido.

## Qué queda (pendientes al día)

1. **🆕 #2 — panel "Nombres en ventas sin asignar" (Admin):** prompt listo `_handoff/PROMPT-CC-nombres-sin-asignar.md`,
   **sin construir**. Auto-detecta nombres del XLS sin `pos_name` y crea+vincula (rol + usuario opcional) en un clic. Sin esquema, sin firma.
2. **📄 Deuda de docs en `main`:** portar `ESTADO.md §(d)` (sumar vista del empleado + reporte a prod), anotar que
   `ventas_nombre` quedó superado por `pos_name`, y la **mig 050** (idempotente, no-op) para repo-truth. Pase de docs aparte.
3. **🧹 `VentasICP.tsx` — 2 casts redundantes** `as { pos_name }` (ahora que `Employee` tipa `pos_name`). Inofensivos.
4. **🧹 Ramas locales** de los pases (`prod/pase-vista-empleado-etapa2`, `prod/pase-reporte-ventas`, `feat/*`, `fix/*`) —
   ya mergeadas; se pueden borrar cuando quieras.

## Ya cerrado esta tanda (2026-07-28/29)
- **Etapa 2 vista del empleado** → prod `73682ab` (ver `HANDOFF-2026-07-28.md`).
- **Paquete del Reporte de Ventas** → prod `1748205`.

## Sagrados / guardrails respetados
`tipCalculations`/`cashUtils` byte-idénticos en todo momento. Cero PoS a prod. Ningún write a la base (todo frontend:
git + GitHub Pages). `main` solo se tocó con la firma del dueño.
