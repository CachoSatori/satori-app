node_modules
dist
# El .env lleva la clave de satori_ro y el INGEST_SECRET: NUNCA al repo.
.env
# Estado local del agente (last_id por local). Es de la PC, no del repo.
.bridge-state.json
*.bridge-state.json
