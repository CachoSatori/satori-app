# HANDOFF — 2026-09-08

Sesión de estabilización + migración de Ventas al PoS. Asesor: Claude. Ejecuta: CC.

---

## 1. Fix de PAX del día — CERRADO

- Commit `d39a788`, en `feat/analitica` e `integracion/analitica-staging`.
- **Bug:** los comensales de tickets cerrados bajo login de caja se acumulaban pero se tiraban al
  construir el `CajeroDay` (no tenía campo `pax`). El total de pax del día no los contaba → PoS
  debajo del xls.
- **Fix (dos escalones):** `armarDia` copia `pax: e.pax` al `CajeroDay`; `getDayStats`/`aggGeneral`
  suman `c.pax`. El pax de caja se queda en caja.
- **Evaluaciones intactas:** `pax` cuenta a todos; `paxSaloneros` (nuevo) es el denominador de
  `promPax`/`bebPax` → el benchmark de cada mesero no cambió.

## 2. Paridad xls ↔ PoS reciente — FIRMADA

- Rango limpio **8–31 agosto 2026**. Neta **−0,4 %** · Salón **−0,0 %** · IVA/Servicio ~0 % ·
  PAX **+1,5 %**. Cuadra.
- **Delivery −4,6 %: diferencia de DEFINICIÓN, no bug.** El xls marca delivery por "sin cargo de
  servicio" (mete para llevar y barra); el PoS usa el canal real (`canal='delivery'`). **El PoS es
  el correcto.** Al migrar, "delivery" baja ~4-5 % legítimo. Documentado en código y `ESTADO.md`.

## 3. Reasignar pax de caja al salonero — DESCARTADO

- De ~29.700 pax de caja: **90 % (26.694) sin mesa**, no cruzable; solo **5 % (1.543)** cruza con
  un salonero, y con ambigüedad.
- **Veredicto:** no vale un SPEC por el 5 %. El 90 % solo se arregla **aguas arriba** (que el
  salonero cargue su pax, o que caja etiquete mesa + salonero).

## 4. P2 — swap de fuente Ventas → PoS — HECHO

- Commit `bbdf122`, `feat/analitica` + integración. Gate verde (1.626 tests, sagrados
  byte-idénticos).
- Lógica en `ventasFuente.ts` (puro, testeable); `VentasModule.tsx` solo llama.
- Flag `FUENTE_VENTAS: 'pos' | 'xls'` (default `pos`; `'xls'` = Excel 100 %, no consulta el PoS,
  con test).
- Fusión `{ ...xls, ...pos }`, **una sola función** para eager/full/hist → `Hoy` y `Análisis` no
  discrepan. El PoS pisa donde hay lote; el Excel queda en 2023 y en los huecos.
- `PRIMERA_JORNADA_POS = '2024-01-05'` — **CONFIRMADO** por
  `VALIDACION-cuadre-pos-agosto-2026-09-04` (backfill 5-ene-2024 → 3-sep-2026).
- 20 tests nuevos load-bearing + 3 de borde en `ventasDiasDesdePos.test.ts`. `xlsParser` / las 3
  funciones del Excel / «Cargar XLS» / las 2 tablas → **latentes**.

## 5. Estado de la HISTORIA al flipear a PoS (leer antes de firmar 2024-2025)

Del doc de validación (2026-09-04), ya medido:

- **Histórico 3 años = 99,94 %.** El PoS da sistemáticamente **−0,057 %**, concentrado en días
  viejos puntuales (casi todo el −107k de mar-2024 está en el **7-mar**). Causa pendiente de
  drill-down (vuelto/dólares viejos, o tipo de factura filtrado en 2024-2025). **NO es borde de
  día.**
- Al correr Paridad sobre 2024-2025 (**criterio 4 de P2**), **esperar esos deltas conocidos**, no
  cuadre al céntimo. Objetivo: confirmar que aparece **solo el −0,057 % conocido y nada nuevo**.
- **Decisión:** para Histórico/Análisis/Mix el −0,057 % es ruido → OK. **Para contable/oficial,
  resolver el drill-down antes.**
- **2024 = 0 % de cobertura de salonero** (el mesero no se registraba; 2025 ~39-66 %, 2026
  ~71-76 %). Saloneros/Evaluación/ICP en 2024 salen vacíos — **no es regresión** (el Excel tenía el
  mismo hueco), pero avisar.

## 6. Pendientes / limpiezas

- **Criterio 4 abierto:** correr Paridad sobre meses de 2024 y 2025 → confirmar solo el −0,057 %
  conocido.
- **Drill-down del −0,057 %** (7-mar-2024) para nombrar la causa — solo necesario si el histórico
  se usa para algo contable.
- **Familia 29 = GREENSEASON** pero el código la llama "Bentos" (`mapTicket.FAMILIA_BENTOS`,
  `CATEGORIA_FAMILIA[29]`, 2 tests). Rename cuando se toque — no toca plata.
- **SPEC descomposición de venta** (`claude/SPEC-descomposicion-de-venta.md`): firmado,
  ⛔ no iniciar hasta después de P2.
- **Afuera de P2:** «En vivo → Hoy» y el retiro del Excel (P4).

---

## Notas del que ejecutó (Claude Code)

Tres cosas que no están arriba y conviene que viajen con el handoff:

1. **La rama de build es `integracion/analitica-staging`, no `staging`.** `staging` está en
   `db690f1` y le falta todo lo de esta sesión, incluida la paginación `bebc9de` sin la cual el
   mix lee líneas truncadas. La integración tiene el árbol **byte-idéntico** a `feat/analitica`.
2. **Dos archivos de test fallan en cualquier corrida de este entorno** y no son de nadie de acá:
   `CashCierre.ajusteTipo` y `salarios.autoderivar`, los dos por variables de entorno de Supabase
   ausentes. Verificado con `git stash` contra el árbol limpio. **No re-diagnosticar.**
3. **El criterio 4 no se puede correr desde acá** (sin acceso a la base). La herramienta exacta ya
   existe y es la pestaña **Paridad (validación)**: compara los MISMOS dos mapas que fusiona
   `ventasFuente.ts`, día por día, con tolerancia configurable. Correrla sobre 2024-2025 **es** el
   criterio 4 — incluido el segundo objetivo, detectar jornadas del PoS presentes-pero-incompletas
   que pisen un xls completo y muestren de menos.
