# HANDOFF — 2026-08-05

> Día de **pases a PROD**: cerró la ola de propinas con la **fuente de verdad `pool_total_crc`** (ítem B)
> y el **bloque de plata (ítems 2 + 3)**. `main` = **`5e85abc`** · `staging` = **`129a516`**. Sagrados
> byte-idénticos, **mig 051 aplicada out-of-band en STAGING y PROD** (sin tocar `schema_migrations`),
> cero `db push`. Todo lo tocado en prod fue **con firma del dueño**.

## ⚡ INICIO RÁPIDO

| | |
|---|---|
| `main` = `origin/main` | **`5e85abc`** — PROD (GitHub Pages, base `/satori-app/`) |
| `staging` = `origin/staging` | **`129a516`** — Cloudflare Pages (base `/`) |
| Sagrados | `tipCalculations` **`7603ba5a`** · `cashUtils` **`b597c697`** · `posFiscal` **`a3fd445f`** (solo-staging) |
| Supabase refs | PROD **`yiczgdtirrkdvohdquzf`** · STAGING **`hwiatgicyyqyezqwldia`** |

**Ponerse al día en 3 lecturas:** [ESTADO.md](ESTADO.md) (el mapa) → **este handoff** (el día) →
[PROMPT-CONTINUACION.md](PROMPT-CONTINUACION.md) (el backlog).

**RITUAL antes de CUALQUIER comando de base:** `cat supabase/.temp/project-ref` (= staging `hwiatgic…`)
**y** `supabase/.temp/linked-project.json` (`name` es el desempate). `db query --linked` **CUELGA** → ir por
Management API (`POST /v1/projects/<ref>/database/query`, token del Keychain, servicio `Supabase CLI`). Para
**PROD, siempre** el canal firmado ([`prod-gate.ts`](scripts/t0-reconciliacion-cajas/prod-gate.ts):
`read_only:true` + smoke `25006` + ref clavado).

**➡️ Próxima tarea recomendada:** **ítem 1 — dirección obligatoria en traspasos** (1a código + 1b datos
prod). Toca plata → **firma del dueño**. (Backlog completo abajo.)

---

## Los pases del día (`main` `2e6fba1` → `5e85abc`)

| # | Merge | Qué |
|---|---|---|
| 1 | **`b36a382`** | Endurecimiento de caja — **ítem 6** (Ingreso adicional: categoría+motivo+umbral ₡100k) + **ítem 4** (guard de fechas: apertura hoy · backdateo por rol). Sin esquema. |
| 2 | **`723f734`** | **`covered_role` canónico** en correo mensual + Estadísticas (Historial ya lo hacía). Correo de Julio **ENVIADO** = **₡2.167.131**. `monthly-report/pool.ts` replica `calcTurno.totalPool` + oracle test. |
| 3 | **`a6192cd`** | **Quincenal** (`TipQuincenal.tsx`) —el último reporte que quedaba en rol base— alineada a `covered_role` + "Total mes" = pool. |
| 4 | **`8c65686`** | **Bloque de plata — ítems 2 + 3** (ver abajo). |
| 5 | **`5e85abc`** | **Ítem B — `pool_total_crc` fuente de verdad** (ver abajo). |

Detalle de 1–3 → [HANDOFF-2026-08-03.md](HANDOFF-2026-08-03.md). Los pases 4 y 5 son de hoy:

### Ítem 4 — bloque de plata (ítems 2 + 3) — merge `8c65686`
- **(2) Prohibir montos negativos en la carga MANUAL** (asistente / nuevo movimiento / edición inline /
  draft de pago = 4 puntos de carga). ⚠️ Los negativos **bicurrency del SISTEMA** son legítimos — la regla
  es solo para la carga **manual**.
- **(3) `ajuste_tipo` del cierre derivado del SIGNO** de la diferencia (etiqueta; el `select` queda
  solo-lectura), en vez de cargarse aparte y poder contradecir al número.
- Rama `feat/plata-negativos-ajustetipo` (staging `6479eb8`) → cherry-pick sobre main → merge **`8c65686`**.
  Choque cosmético del ítem 8 auto-resuelto conservando "la dueña". **Motor/matemática intactos**; prueba
  byte-perfecta = solo 4 líneas dueño/dueña. Pages verificado por chunk.

### Ítem 5 — `pool_total_crc` fuente de verdad — merge `5e85abc`
El total real del pool de un turno = `calcTurno/calcHistory().totalPool` (SAGRADO): efectivo + **propinaSala
por `covered_role`** + barra (ef+elec). Las columnas `pool_*` de la sesión no alcanzan para reconstruirlo sin
releer `tip_entries` (les falta la `propinaSala`) → la fila colapsada de Historial **subcontaba**. Ahora:
- **mig 051** — `ALTER TABLE tip_sessions ADD COLUMN IF NOT EXISTS pool_total_crc numeric` (nullable, sin default).
- **Escritura al cerrar/editar** — `updateSessionPools` acepta `pool_total_crc` opcional; se persiste en el
  MISMO update al cerrar (`TipsModule.handleCloseSession` → `totals.totalPool`) y al editar
  (`TipHistory.handleSaveEdit`). El autoguardado de pools NO lo pisa.
- **Lectura** — `poolTotalOf(s) = s.pool_total_crc ?? (fallback)`; Historial muestra el total real **sin
  expandir**. `getTipSessions` ya lo trae por `select('*')`.
- **Backfill** ([`scripts/pool-backfill/`](scripts/pool-backfill/README.md)) — envuelve el SAGRADO
  `calcHistory` (no reimplementa), idempotente, `--dry-run` por default; escribe **SOLO** `pool_total_crc`.
- **Orden del pase (innegociable):** mig → backfill → código. Aplicado a prod con firma: mig 051 out-of-band
  → backfill de las **266 sesiones** (writer de un solo uso, batched por el rate-limit, checksum de las demás
  columnas idéntico antes/después) → merge del código. **Julio 2026 = ₡2.167.131** sumado desde
  `pool_total_crc`. El cherry-pick se hizo en un **git worktree aislado** para no tocar el árbol de staging.

**Gate de cada pase:** `VITE_APP_ENV=production` build EXIT 0 · suite verde · ESLint tocados sin nuevos ·
sagrados byte-idénticos. Pages verificado **por chunk** (nunca por HTTP 200): `version.json` = hash del
merge + el string de la feature presente en el chunk servido.

---

## 🧾 Invariante de propinas — CERRADA

La clasificación **sala↔barra por `covered_role`** (el rol que la persona CUBRIÓ ese turno, canónico —
decisión del dueño) es uniforme en los **6 consumidores** del pool, y **`pool_total_crc` es la fuente única**
del total:

1. **Cierre del turno** — `TipsModule` → `calcTurno` con coberturas aplicadas.
2. **Edición** — `TipHistory.handleSaveEdit` → `calcTurno` con rol efectivo.
3. **Historial** — `TipHistory` → `calcHistory`; y ahora **lee `pool_total_crc`** sin recomputar.
4. **Estadísticas** — `TipStats` pasa `covered_role` a `calcHistory`.
5. **Quincenal** — `TipQuincenal` (alineado en `a6192cd`).
6. **Correo mensual** — `monthly-report/pool.ts` (réplica del sagrado, atada por oracle test).

**Julio 2026 = ₡2.167.131** en todos (correo · Estadísticas · Quincenal · Historial · `Σ pool_total_crc`).
Invariante de conservación **pool == Σ payout ±₡1** confirmada (61/61 sesiones).

---

## 🗃️ Estado de migraciones

- **mig 051** (`pool_total_crc` en `tip_sessions`, nullable) **aplicada OUT-OF-BAND en STAGING y PROD** vía
  Management API (`read_only:false`) — **NO** en `schema_migrations` (el `db push` sigue **frenado por el
  ledger**, ver ESTADO §c). Idempotente. Backfill: **266 sesiones prod / 2 staging**, **solo `pool_total_crc`**
  (checksum de las demás columnas idéntico; `updated_at` bumpea por trigger — inherente al UPDATE, no es
  columna de negocio).
- **⚠️ COLISIÓN DE NÚMERO:** la rama **`metas_personales`** también reserva **051**. En staging/main el 051
  ya está TOMADO por `pool_total_crc` → **renumerar `metas_personales` a `052+`** antes de que vaya a staging.

---

## 🔀 Divergencia ESPERADA (no es drift)

- **Ítem 8 cosmético** ("la dueña"→"el dueño", `6b7a7bc`): **solo en staging**, DIFERIDO en prod →
  `AgregarAsistente.tsx` difiere main↔staging por **2 comentarios**. Converge cuando el ítem 8 pase a prod.
- **PoS completo** (comandero/KDS/cobro/FE SIM/inventario COGS, migs 022–037): **solo en staging**, bloqueado
  por el pilar de auth. `posFiscal.ts` **no existe** en `main` (por eso su hash se chequea solo en staging).

---

## 📋 Backlog (ordenado)

1. **🖊️ Ítem 1 — dirección obligatoria en traspasos (1a + 1b).** Exigir origen→destino en el alta
   (1a, código) + resolver los `'Otro traspaso'` existentes (1b, datos prod). **Toca plata → firma.** ← **PRÓXIMO**
2. **🖊️ Ítem 5 — auditoría de ediciones** de movimientos (hoy solo borrados vía `movement_deletions`; la
   edición pasa por reemplazo y no deja el mismo rastro). **Puede tocar esquema.**
3. **🧹 Ítem 7 — 2 huérfanos** de fecha imposible: `9b79e731` (2020-07-09, ₡74.126,92) y uno de **2016**
   (₡54.978). La app NO los muestra (`getAllCashMovements(days=1000)` arranca ~1.000 días atrás). Borrar vía
   `delete_movement_cascade`.
4. **🎨 Ítem 8 → prod** — cosmético "la dueña"→"el dueño" (`6b7a7bc`, ya en staging). **Excluir sagrados.**
   Haría converger `AgregarAsistente.tsx` main↔staging.
5. **👁️ Mejora UI (letra chica)** — en Estadísticas, "generado por cobertura" cuando alguien cubrió otro
   puesto ese turno (para explicar por qué su clasificación de pool cambió).

---

## 🌿 Ramas podables

- **Trabajo ya en prod Y staging** (mergeado, seguro de borrar): `feat/pool-total-persistido` (`129a516`,
  = `staging`) · `feat/plata-negativos-ajustetipo` (`6479eb8`) · `fix/quincenal-covered-role` (`dad7ea9`) ·
  `feat/reporte-propina-electronica` (`7d768d1`) · `feat/guard-fechas-caja` · `feat/ingreso-adicional-control`.
- **Ramas throwaway de pase** (ya en `main`): `prod/pase-caja-6-4` · `prod/pase-covered-reportes` ·
  `prod/pase-quincenal-covered` · `prod/pase-plata-2y3`. (`prod/pase-pool-total` ya se borró post-merge.)
- Hay **~100 ramas locales históricas** `in-staging` — podables en lote aparte, no urgente.

---

## 🛡️ Guardrails respetados

Solo **docs a staging** en este cierre. **`main` NO se tocó** (sigue **`5e85abc`**). Sagrados byte-idénticos
en todo momento. El backfill escribió **SOLO `pool_total_crc`**; `schema_migrations` intacto; el link del CLI
de Supabase se mantuvo/devolvió a **staging**. Prod solo se tocó con **firma del dueño**.

**Rollback del ítem B:** `git revert -m 1 5e85abc` + push (la columna y los datos quedan **inertes** sin el
código; o `UPDATE tip_sessions SET pool_total_crc = NULL` para revertir el dato).
