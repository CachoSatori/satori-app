# HANDOFF — 2026-07-30

> Sesión del **panel "Nombres en ventas sin asignar" (Admin)** — backlog **#2**, que estaba **SIN CONSTRUIR**.
> Construido, validado en piso por el dueño (staging) y **pasado a PROD con firma** — ✅ **live y verificado por
> contenido**. Cambio de **1 archivo** (`admin/EmployeeList.tsx`), display + writes vía APIs existentes. **Cero
> esquema, cero plata.** Sagrados intactos. Continúa el handoff del 2026-07-29 (paquete Reporte de Ventas en prod).

## Estado al cerrar

| | |
|---|---|
| `origin/main` | **`8d41d1a`** — merge `--no-ff` del pase (cherry-pick `863c900` de `1969f8f`) sobre `1748205`. Deploy GitHub Pages ✅ verificado **por contenido** (`version.json`=`8d41d1a`; chunk `AdminModule-DshuGeA3.js` con "Nombres en ventas sin asignar" / "Crear + vincular" / "sin usuario" / "no están vinculados"). |
| `origin/staging` | **`1969f8f`** — mismo commit (fuente de la validación), ff sobre `fc2c95b`. |
| Scope del pase | **1 archivo** (`EmployeeList.tsx`, +136/−0), **cero PoS**, byte-idéntico main↔staging |
| Sagrados | `tipCalculations` `7603ba5a` · `cashUtils` `b597c697` — **intactos** (`posFiscal` no existe en main) |
| Gates | `build` EXIT 0 · tests: **staging 504/504**, **prod 424/424** (baseline main; el panel no agrega tests) · ESLint limpio |
| Rollback | `git revert -m 1 8d41d1a && git push origin main` — sin esquema |

## Qué se hizo (panel "Nombres en ventas sin asignar")

Panel en **Admin → Empleados**, arriba de la tabla, colapsable, visible solo si hay pendientes. Lista los nombres
del XLS de ventas que **no** son el `pos_name` de ningún empleado
(`unassignedNames = salNames.filter(n => !employees.some(e => (e.pos_name ?? '') === n))`, ordenado). Por cada nombre:
selector de **rol** (default `salonero`), **usuario opcional** (perfiles no vinculados, sin owner/contador) y botón
**"Crear + vincular"**:

- Si ya existe un empleado con ese `full_name` (case-insensitive) **sin** `pos_name` → **lo vincula** (`updateEmployee`
  con `pos_name`), **NO duplica**.
- Si no existe → `createEmployee({ full_name: nombre.toUpperCase() })` → `updateEmployee(id, { pos_name })` con el
  **valor EXACTO del XLS** (sin uppercase, para el match de Mi Rendimiento) → link de usuario si se eligió.
- `onRefresh()` al terminar; en error, si ya creó, refresca igual para **no duplicar** en un reintento.

Reusa `createEmployee`/`updateEmployee`/`linkProfileToEmployee` **sin cambiar firmas**. No toca la tabla, el editor ni
`BulkImport`. Diff **puramente aditivo**.

**Commits:** staging `1969f8f` (ff `fc2c95b`→`1969f8f`) · prod cherry-pick `863c900` → merge `--no-ff` **`8d41d1a`**.

> 🧹 **Nota operativa:** al arrancar la sesión había un `git merge --no-ff prod/pase-vista-empleado-etapa2` colgado
> (PID viejo del pase Etapa 2 del 28/07) que retenía `.git/index.lock`. Se confirmó que su resultado ya vivía en prod
> (`73682ab`, la rama ya era ancestro de main) → cruft. Se mató el proceso y se limpió el lock; `git status` quedó
> limpio antes de arrancar. Sin pérdida.

## Qué queda (pendientes al día)

1. **📄 Deuda de docs en `main`** (pase de docs con firma — toca main): portar `ESTADO.md §(a)/§(c)/§(d)` (sumar vista
   del empleado + reporte + panel a prod; `main` código sigue documentado como `1c8a9ad`/`c77ced0`), anotar que
   `ventas_nombre` quedó **superado por `pos_name`**, y registrar la **mig 050** (`employee_pos_name`, idempotente,
   no-op — la columna ya vive en prod) para repo-truth.
2. **🧹 `VentasICP.tsx` — 2 casts redundantes** `as { pos_name }` (~líneas 83 y 331), ahora que `Employee` tipa
   `pos_name`. Inofensivos. Staging, sin firma.
3. **🧹 Ramas locales ya mergeadas** que se pueden borrar: `prod/pase-nombres-sin-asignar`, `feat/mi-rendimiento-*`,
   `feat/vinculo-empleado-posname`, `feat/ruteo-propinas-bandeja`, `prod/pase-*` y `feat/reporte-*` viejas.

## Sagrados / guardrails respetados

`tipCalculations`/`cashUtils` byte-idénticos en todo momento. **Cero PoS a prod** (port selectivo por cherry-pick, NO
merge de staging → los ~103 archivos de divergencia con el PoS quedaron fuera). Ningún write a la base (pase puro
frontend: git + GitHub Pages). `main` solo se tocó con la **firma del dueño**.
