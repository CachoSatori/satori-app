# HANDOFF — 2026-07-30

> **Dos pases a PROD hoy**, ambos verificados:
> 1. **Panel "Nombres en ventas sin asignar" (Admin)** — feature, 1 archivo (`admin/EmployeeList.tsx`). → prod `8d41d1a`.
> 2. **Deuda de docs a repo-truth** — docs + 1 `.sql` no-op, **cero código/PoS/DB**. → prod `f5e7eb5`.
>
> **+ limpieza en staging (sin firma):** quitados 2 casts redundantes en `VentasICP.tsx` — commit `964a832`, FF-merge a staging.
>
> Estado final del día: **`origin/main` = `f5e7eb5`** · **`origin/staging` = `964a832`**. Sagrados intactos en todo momento.

---

## Pase 2 (cierre del día) — Deuda de docs a repo-truth · `f5e7eb5`

> Traer `ESTADO.md` y `PROMPT-CONTINUACION.md` a la verdad de prod de los 3 pases de jul-2026 y
> registrar la mig `050`. **Cero código de app, cero PoS, cero DB.** Construido en rama desde `main`,
> revisado por el asesor, **firmado por el dueño**, mergeado `--no-ff` y deployado.

| | |
|---|---|
| `origin/main` | **`f5e7eb5`** — merge `--no-ff` de `prod/pase-docs-repotruth` (commit `95242d4`) sobre `8d41d1a`. Parent1 = `8d41d1a` (rollback limpio). |
| Scope | **exactamente 3 archivos**: `ESTADO.md`, `PROMPT-CONTINUACION.md`, `supabase/migrations/050_employee_pos_name.sql`. **Cero código, cero PoS.** |
| Sagrados | `tipCalculations` `7603ba5a` · `cashUtils` `b597c697` — **intactos** (blob idéntico antes/después) |
| Gates | `build` EXIT 0 · **424/424** tests (baseline de main) · ESLint = baseline (0 código tocado; el config lintea solo `**/*.{ts,tsx}`) |
| Deploy | GitHub Pages run `30578495951` → **success** · `version.json.commit` = `f5e7eb5`. Como no se tocó app, los chunks servidos son idénticos — solo se movió `version.json`. |
| Rollback | `git revert -m 1 f5e7eb5 && git push origin main` |

### Qué entró
- **`050_employee_pos_name.sql`** (nuevo, blob `956570d`, byte-idéntico a staging): repo-truth de `employees.pos_name`. Idempotente (`add column if not exists`), **no-op** — la columna ya vive out-of-band en prod y staging. **NO aplicada por el ledger** (no cuenta entre las 33 filas de prod); espera un `db push` **firmado** si alguna vez se corre.
- **`ESTADO.md`**: header `2026-07-30` · §(a) `main`=`8d41d1a` (hash code-bearing; la app no cambió) / `staging`=`1b79265`, RITUAL corregido (`linked-project.json` **sí existe**, `name`=`satori-staging` es el desempate), merge `main→staging` marcado **hecho 23/07** · §(c) ledger reconciliado **B2** (prod 33 filas) + bullet de la mig 050 · §(d) fila de los 3 features ✅ EN PROD + nota **`ventas_nombre` SUPERADO por `pos_name`** · §(f).1/.2 marcados ✅. **§(b) y la tabla de Sagrados NO se tocaron.**
- **`PROMPT-CONTINUACION.md`**: header con hashes al día + **"deuda de docs SALDADA"** · P1.1 (merge) y P1.2 (ledger) marcados ✅.

### Verificación del asesor (independiente, contra el repo)
`origin/main` = `f5e7eb5`, merge parent1 = `8d41d1a`; `git diff 8d41d1a..main` = solo los 3 archivos; sagrados por blob `7603ba5a`/`b597c697`; sin links rotos (dropeó `FASE-A-LEDGER`, backups `ledger-*.json`, `HANDOFF-2026-07-27` que no existen en main; los links que quedan resuelven). Contenido veraz y consistente entre ambos docs.

### Nota
- Se corrigió una memoria desactualizada: `supabase/.temp/linked-project.json` **sí existe** (`name`=`satori-staging`).
- Los docs leen `main = 8d41d1a` a propósito: `f5e7eb5` es el merge docs-only que *añade* los propios docs (un archivo no puede contener el hash de su propio merge); como `f5e7eb5` no trae código, `8d41d1a` sigue siendo el hash **code-bearing** correcto. No es drift.

---

## Pase 1 — Panel "Nombres en ventas sin asignar" (Admin) · `8d41d1a`

Panel en **Admin → Empleados**, arriba de la tabla, colapsable, visible solo si hay pendientes. Lista los nombres
del XLS de ventas que **no** son el `pos_name` de ningún empleado
(`unassignedNames = salNames.filter(n => !employees.some(e => (e.pos_name ?? '') === n))`, ordenado). Por cada nombre:
selector de **rol** (default `salonero`), **usuario opcional** (perfiles no vinculados, sin owner/contador) y botón
**"Crear + vincular"**:

- Si ya existe un empleado con ese `full_name` (case-insensitive) **sin** `pos_name` → **lo vincula** (`updateEmployee`
  con `pos_name`), **NO duplica**.
- Si no existe → `createEmployee({ full_name: nombre.toUpperCase() })` → `updateEmployee(id, { pos_name })` con el
  **valor EXACTO del XLS** (sin uppercase, para el match de Mi Rendimiento) → link de usuario si se eligió.
- `onRefresh()` al terminar; en error, si ya creó, refresca igual para **no duplicar** en un reintento.

Reusa `createEmployee`/`updateEmployee`/`linkProfileToEmployee` **sin cambiar firmas**. No toca la tabla, el editor ni
`BulkImport`. Diff **puramente aditivo** (1 archivo, +136/−0). Deploy verificado por contenido (chunk `AdminModule-DshuGeA3.js`).

**Commits:** staging `1969f8f` (ff `fc2c95b`→`1969f8f`) · prod cherry-pick `863c900` → merge `--no-ff` **`8d41d1a`**.

## Limpieza en staging (Pase 1.5, sin firma) — VentasICP

Quitados los **2 casts redundantes** `as { pos_name?: string | null }` en `VentasICP.tsx` (~líneas 83 y 331): `emp` ya
viene narrowed a `Employee`, y `Employee` **ya tipa** `pos_name: string | null` ([database.ts:49]), así que el cast
sobraba. `posName` pasa de `string | null | undefined` a `string | null` — **cambio puramente de tipos, cero runtime**.
Gates: `build` EXIT 0 (`tsc -b` real acepta la remoción → prueba de que sobraban) · **504/504** tests (baseline staging) ·
ESLint del archivo 0 errores (1 warning pre-existente en la línea 97, ajeno). Sagrados intactos. Commit `964a832`,
FF-merge a `staging` + push. **Cero main/prod.**

## Qué queda (pendientes al día)

1. **✅ HECHO (staging) — `VentasICP.tsx` casts redundantes** quitados (commit `964a832`, FF a `origin/staging`). Ya no pendiente.
2. **✅ HECHO — poda de ramas locales mergeadas en `origin/main`:** 33 ramas borradas con `git branch -d` (todos los
   `prod/pase-*`, `hotfix/*-prod`, `port/*`, `fix/*-a-main`, `chore/*-a-main` cuyo contenido ya está en `main`). **Quedan
   ~110 `feat/*` cherry-pickeadas** (`feat/mi-rendimiento-*`, `feat/reporte-*`, `feat/vinculo-*`, `feat/ruteo-*`…): NO son
   ancestros de `main` (entraron por cherry-pick), necesitan **triage por rama** en otra sesión → NO se borraron.
3. **(P0 de fondo)** T3 — endurecimiento de caja (plata, firma, sesión propia). No urgente.

> ✅ **Deuda de docs de repo-truth: SALDADA** (Pase 2). `ESTADO.md`/`PROMPT-CONTINUACION.md` ya reflejan prod.

> 🧹 **Nota operativa (cruft de git, recurrente):** al commitear en staging apareció otra vez un `.git/index.lock` colgado
> + un `vi` huérfano sobre `.git/MERGE_MSG` (de un merge abandonado días atrás; el `MERGE_MSG` ya estaba borrado en disco,
> sin `MERGE_HEAD` → sin merge en curso). Fix seguro: verificar que NO hay `MERGE_HEAD` ni proceso git vivo, y `rm -f
> .git/index.lock`. El `vi` huérfano (PID viejo) quedó sin matar por ser inofensivo; conviene cerrarlo cuando se pueda.

## Sagrados / guardrails respetados
`tipCalculations`/`cashUtils` byte-idénticos en todo momento. **Cero PoS a prod** en ambos pases. Ningún write a la base
(los dos pases: git + GitHub Pages; la limpieza de VentasICP: solo git a staging). `main` solo se tocó con la **firma del dueño**.
