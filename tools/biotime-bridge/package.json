{
  "name": "biotime-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agente local BioTime -> Supabase (Satori F1c). Lee iclock_transaction en solo lectura y empuja las marcas a la Edge Function ingest-punches.",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
