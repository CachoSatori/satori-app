# Satori App — Estado del proyecto

> Restaurant POS + analítica · Satori Sushi Bar, Santa Teresa & Nosara, Costa Rica
> **Handoff: 2026-07-06. ✅ OLA 2026-07 CERRADA — pase a prod + smoke físico + sinceramiento USD, todo completo.** El pase único quedó **validado en piso por el dueño** (smoke en PROD ✓: `version.json` ✓ · Caja Diaria sin errores · asistente foto + lectura IA Sonnet — efectivo y pendientes, genera tarea de revisión · borrado con contraseña de manager elimina movimiento + tarea · Cierre del Día con diferencias USD y gate de ajuste · propinas sin parpadeo, pago por la vía real) y el **sinceramiento USD de Caja Fuerte se realizó en prod** ✓. **`main` código = `6c65f25`** (post-pase propinas ef/elec + cierre 2026-07-09/10; antes `92c0831`) · **`staging` código = `1daef0c`** (migs PROD out-of-band: **≤021 + 038–046 + subset core de la 026**; secret **`ANTHROPIC_MODEL=claude-sonnet-4-5` en prod**). Los HEAD de ambas ramas avanzan solo por commits **docs-only** desde entonces. **➡️ Ahora: ESTABILIZACIÓN** (1–2 semanas de observación, sin construir).
>
> **En una línea:** prod dejó de ir ~143 commits atrás — ahora tiene Caja/Cierre/USD/Revisión/asistente + Bandeja + unificación completos; lo único que queda solo-en-staging es el **PoS** (comandero/KDS/cobro/FE/inventario activo), diferido y bloqueado por el pilar de auth.
>
> **🆕 2026-07-09 — PASE A PROD de 2 features (validadas en piso en staging).** Vehículo: rama **`prod/pase-propinas-cierre`** sobre `main` (`cf50724`) — cherry-pick **limpio** de 3 commits (`611afca` → `2a0852e` → `c26df15`; solo ESTADO/PROMPT-CONTINUACION divergían y se resolvieron con la versión de `main`). Las 2 features: **(i) propinas efectivo/electrónico** (la cuenta por pagar de propinas es **SOLO lo electrónico** — datáfono/SINPE; el efectivo se lo queda el equipo, nunca genera movimiento ni pendiente; el *take-home* no cambia) y **(ii) cierre del día: ventas ₡0 con confirmación explícita + modal de resumen antes de confirmar**. Gates verdes: `VITE_APP_ENV=production npm run build` EXIT 0 · **207 tests sin env** · `tipCalculations.ts`/`cashUtils.ts` **byte-idénticos a `main`**. **Esquema:** la **mig 046** (`tip_sessions.pool_barra_electronico_crc`, aditiva/idempotente) se aplica a **PROD out-of-band** (Management API + `NOTIFY pgrst,'reload schema'`; `schema_migrations` NO se toca → prod pasa a **038–046 + subset core de 026**), y fue **ANTES** del código (si no, `getTipPayoutsSince` rompe pidiendo una columna inexistente). **✅ COMPLETADO 2026-07-10:** (1) mig 046 aplicada a prod out-of-band (verificada: columna `numeric NOT NULL default 0`; `schema_migrations` intacto) · (2) merge FF a `main` (**`main` código ahora = `6c65f25`**) + deploy GitHub Pages (`version.json` live = `6c65f25`) · (3) **SMOKE EN PISO EN PROD ✓ (2026-07-10)** — el dueño validó el cierre real con las 2 features (solo el electrónico genera cuenta por pagar · cierre ventas-0 + resumen OK). **Las 2 features salen de la cola de pendientes.** Queda solo la deuda ya conocida: **reconciliación del ledger** (prod out-of-band **038–046 + subset 026**; staging **039–046**). Rollback si hiciera falta: revert de los commits + redeploy (la 046 queda, aditiva e inofensiva). Detalle → PROMPT-CONTINUACION.
>
> **🆕 2026-07-16 — PASE CORTO A PROD: Proveedores, el rojo = DEUDA REAL (validado en staging).** Vehículo: rama **`prod/pase-proveedores-rojo`** sobre `main` (`6eb939a`) — cherry-pick de `f7bff9a` (staging); **solo ESTADO/PROMPT-CONTINUACION divergían** y se resolvieron con la versión de `main`; **el código entró limpio**. **SIN esquema — solo código** (4 archivos). Qué cambia: **(a)** el badge **rojo** de Proveedores cuenta **deuda real registrada** (`pendCount` = movimientos `status='pendiente'`, **incluye los 2 huérfanos** `supplier_id` NULL) — antes mostraba `overdueCount` con la etiqueta engañosa "pagos pendientes"; **(b)** la **agenda de ciclo** pasa a un **indicador ámbar aparte** ("N con ciclo de compra vencido"), nunca rojo — es agenda de recompra, no deuda; **(c)** proveedor **'Puntual'** (nuevo valor de `ciclo_pago`, **texto libre, sin migración**) queda **fuera de la agenda** → **mata el "14"**; **(d)** la pestaña **Pendientes** ahora tiene **✕ Rechazar** (exige **autorización de gerencia**; anda con `supplier_id` NULL porque agrupa por nombre y rechaza por `id`). Gates verdes: `VITE_APP_ENV=production npm run build` **EXIT 0** · **218 tests sin env** · **diff de sagrados vs `main` VACÍO** · los 4 archivos **byte-idénticos** a los smokeados en staging. **✅ SMOKE EN PROD SALDADO (2026-07-16):** el dueño **rechazó los 2 huérfanos** él mismo desde la UI en prod — Distribuidora Isleña 2020-07-09 ₡74.126,92 y GRUPO PAMPA 2026-07-06 ₡75.916,60 (**₡150.043,52**, **cero SQL**) → **pestaña Pendientes 5→3**, validado en piso. (El "ver el rojo en 5" quedó *moot*: el 2º pase del mismo día retiró el badge rojo — la deuda vive ahora en la tarjeta de cada proveedor.) Rollback: **revert del commit** + redeploy (sin migración → trivial). Detalle → PROMPT-CONTINUACION P1 #1 + HALLAZGOS.
>
> **🆕 2026-07-16 (2º pase del día) — PASE CORTO A PROD: Proveedores = SOLO la lista (OK visual del dueño en staging).** Vehículo: rama **`prod/pase-proveedores-simplificar`** sobre `main` (`7377240`) — cherry-pick de `d85453a` (staging); **solo ESTADO/PROMPT-CONTINUACION divergían** → versión de `main`; **el código entró limpio**. **SIN esquema — solo UI** (3 archivos; **125 líneas menos**). **Decisión de producto FIRMADA por el dueño el mismo día, tras ver el pase anterior en prod:** la pestaña Proveedores es **SOLO la lista de proveedores**. **Se eliminan:** el badge **rojo** "N pendientes por pagar" + su panel (**era duplicado** — la pestaña Pendientes, al lado, ya notifica con su propio `cd-pend-badge`, `CashModule.tsx:135`), el **chip ámbar** de agenda de ciclo, y los **banners** "Agenda de compra". **La información NO se pierde: vive en la tarjeta de cada proveedor** (su deuda `pendingCRC`, su ciclo acordado —incluida **'Puntual'** y su semántica—, último/próximo pago, total pagado). **`CashPendientes` intacto** (✕ Rechazar sigue → el rechazo de los 2 huérfanos sigue disponible). **Sin código muerto:** `contarAgenda`/`contarPendientes`/`totalPendienteCRC` quedaron sin uso (ningún otro módulo los importaba) → borrados con sus tests; `computeSupplierStatus`/`esProveedorPuntual` siguen (los usan las tarjetas). Gates verdes: `VITE_APP_ENV=production npm run build` **EXIT 0** · **216 tests sin env** · **diff de sagrados vs `main` VACÍO** · los 3 archivos **byte-idénticos** a los aprobados en staging (`d85453a`, OK visual del dueño). Rollback: **revert del commit** + redeploy (sin migración → trivial). Detalle → PROMPT-CONTINUACION P1 #1.
>
> **🔥 2026-07-17 — HOTFIX A PROD: el buscador de Movimientos crasheaba la pantalla al tipear.** Vehículo: rama **`prod/pase-hotfix-buscar`** sobre `main` (`127cc7a`) — cherry-pick de `60150c4` (staging; ya en `satori-staging.pages.dev`, sin regresión visible); **solo ESTADO/PROMPT-CONTINUACION divergían** → versión de `main` + esta entrada re-aplicada a mano; **el código entró byte-idéntico** (3 archivos). **Síntoma:** en Caja → Movimientos, tipear en "Buscar..." tiraba la pantalla al **ErrorBoundary**. **Causa raíz — bug LATENTE de datos, NO de los pases del 16:** el filtro llamaba `.toLowerCase()` **directo** sobre `m.supplier_name`/`m.employee_name`, que son **NULLABLE** en la base (`supabase.gen.ts`: `string | null`) y tienen filas viejas con `null`; pero **`database.ts` los declaraba `string` a secas → el tipo MENTÍA y TS nunca avisó**, y `getCashMovements` castea la fila cruda (**`data as CashMovement[]`**) → el `null` llega intacto al componente. Los caminos de **escritura** sí coercionaban (`?? ''`), por eso **solo revientan los datos viejos** → prod crasheaba y staging no (su espejo de datos es viejo, no el código). **Fix (solo código, cero esquema):** **(1)** filtro null-safe (`(m.supplier_name ?? '').toLowerCase()`, ídem `employee_name`/`description`); **(2)** el tipo dice la verdad (`supplier_name`/`employee_name` → **`string | null`**) → previene la reincidencia; **(3)** cascada mínima — **1 solo sitio** (`defaultValue` de un input, `null || null` → cerrado con `|| ''`), **cero lógica de plata**. Grep general: **0 lectores inseguros restantes** (`CashPendientes`, `InboxModule`, `CashTurno`, `FinanzasModule` ya usaban `||`/`??`). Gates verdes: `VITE_APP_ENV=production npm run build` **EXIT 0** · **219 tests sin env** (216 baseline de `main` + 3 del test nuevo) · **diff de sagrados vs `main` VACÍO** · **test nuevo verificado que FALLA sin el fix** (el `TypeError: Cannot read properties of null (reading 'toLowerCase')` exacto de prod). **`main` código pasa a este commit** + deploy GitHub Pages (`version.json`). Rollback: revert del commit + redeploy (sin migración → trivial). Detalle → HALLAZGOS.
>
> **🆕 2026-07-17 — QUICK-WINS C2 + C3 + BUSCADOR DE PROVEEDORES A PROD** (research quick-wins FIRMADOS; smoke del dueño en staging **3/3 ✓**: historial over/short con datos reales · buscador · email del cierre recibido). Rama **`prod/pase-quickwins`** sobre `main` (`c3d8070`) — cherry-pick de `8c41965` (staging); **solo ESTADO/PROMPT-CONTINUACION divergían** → versión de `main` + entradas re-aplicadas a mano; **el código entró byte-idéntico** (8 archivos). **SIN esquema · sagrados intactos (diff VACÍO) · cero migraciones.** **C2 — historial de sobrantes/faltantes del cierre** (patrón Toast over/short): `cierreStats.ts` (lógica pura, **display de lo ya sellado en `cash_cierres_dia`, NO recalcula**; null-safe porque `diferencia_crc`/`ajuste_*` son NULLABLE en la base) + sección nueva en `CashResumen` (respeta su filtro por mes): por cierre completo → fecha · diferencia ₡ con signo/color (sobrante/faltante/cuadró) · ajuste tipo/motivo; + agregados del período (cuántos cuadraron vs no · neto · absoluto). **C3 — email del cierre al owner** (patrón Square): al confirmarse el cierre COMPLETO, `CashCierre` dispara **`sendCierreEmail` fire-and-forget** (si el email falla, el cierre NO se rompe — la plata manda). Edge Function nueva **`cierre-email`** con la lección del **hallazgo de seguridad #2: EXIGE `Authorization` (JWT verificado) y relee la fila server-side con el token del usuario (RLS = portón), NO `service_role`** — mismo patrón que `extract-document` post-IDOR. HTML = mismo resumen del popup. **✅ Deployada a PROD** (`--project-ref yiczgdtirrkdvohdquzf` = "satori-app", con doble-check consciente). **`RESEND_API_KEY` ya existía en prod** (por monthly-report) → **secrets NO tocados**. **`REPORT_TO_EMAIL` NO se seteó en prod a propósito:** el default de la función es `cachorrogp@gmail.com` (hoy la única dirección que Resend entrega con el sender sandbox) → **monthly-report queda intocado**; el cambio de destinatario va entero con la **tarea de DNS**, aparte. **➕ EXTRA (pedido del dueño): buscador en Proveedores** — filtro en vivo por nombre/categoría/contacto, **null-safe desde el día uno**, estado vacío "Sin coincidencias". Gates: **build prod EXIT 0 · 231 tests sin env · sagrados VACÍO · cero migraciones**. **`main` código pasa a este commit** + deploy GitHub Pages (`version.json`). **⏳ SMOKE REAL EN PROD: el cierre completo de esta noche manda el primer email solo** (a `cachorrogp@gmail.com`). Rollback: revert del commit + redeploy (sin migración → trivial); la función queda desplegada e inerte si no se usa. Detalle → PROMPT-CONTINUACION.
>
> **🆕 2026-07-17 — PASE A PROD: Mi Rendimiento (la "casa del empleado" + ICP electrónico), validado en piso en staging.** Vehículo: rama **`prod/pase-mi-rendimiento`** sobre `main` (`880c863`) — cherry-pick **limpio** de 2 commits (`2251d05` → `be2cb50`; **cero conflictos**, no tocan ESTADO/PROMPT). **SOLO FRONTEND · SIN esquema · cero migraciones** (`tips.ts` solo agrega `tip_amount_crc/usd` + `exchange_rate` al SELECT de `getAttendanceHistory` — **columnas que YA existen en prod**). Qué entra: **(a)** `/mi-rendimiento` rehecho como **hub claro del empleado** (tema de Caja, mobile-first) con **filtro de período GLOBAL** (Hoy·Semana·Mes·Rango, reemplaza el fijo-60-días) + 6 pestañas (Resumen·Por día·Productos·Semana·Propinas·Competencias); **(b)** **Mis Propinas integrada** como pestaña (`/mis-propinas` **redirige**) con selector de mes + **ICP ELECTRÓNICO** = propina electrónica **GENERADA** (`tip_amount_crc + tip_amount_usd×TC`) / ventas × 100 + benchmark del equipo — **NO el reparto del pool**: el take-home (`payout_crc`) se muestra aparte, no se conflan; **(c)** roles sin venta (cocina/runner/barback) arrancan propinas-primero. Gates verdes: **build prod EXIT 0 · diff de sagrados vs `main` VACÍO** (`tipCalculations`/`computeTotals`(posFiscal)/`cashUtils` byte-idénticos) · **suite verde salvo el flake TZ conocido de `CashMovimientos.buscarNull`** (falla de noche en CR por `new Date` UTC vs `todayCR`; preexistente, módulo cash, ajeno a este pase). **`main` código pasa a este pase** + deploy GitHub Pages (`version.json`). Rollback: revert de los 2 commits + redeploy (sin migración → trivial). Detalle → HANDOFF-MI-RENDIMIENTO.md.
>
> **🆕 2026-07-18 — PASE A PROD: elegibilidad de propina por ROL (configurable desde Admin), validada en staging.** Vehículo: rama **`prod/pase-elegibilidad-propina`** sobre `main` (`70d7ef2`) — cherry-pick **limpio** de 2 commits (`b1a85ba` → `94991cd`; **cero conflictos**). Cambio **FRONTEND + BASE + dato de PLATA** → se ejecutó en **orden estricto**. **(1) BASE ANTES DEL CÓDIGO:** la **mig 048** (`role_tip_points.recibe_propina boolean not null default true`, aditiva/idempotente) se aplicó a **PROD out-of-band** (Management API sobre el ref **`yiczgdtirrkdvohdquzf`** + `NOTIFY pgrst,'reload schema'`; **`schema_migrations` NO se toca** → prod pasa a **038–046 + 048 + subset core de 026**). Verificado: columna `boolean NOT NULL default true` y **los 7 roles en `true`** → **cero cambio de comportamiento** al aplicarla. **(2) CÓDIGO:** "recibir propina" deja de ser hardcode y pasa a ser **configuración por rol**: toggle **"Recibe propina"** en Admin → Puntos por rol (persiste puntos+flag juntos con `upsertRoleTipConfig`, upsert `onConflict 'role'`), y el **roster del turno** excluye los roles no-elegibles (helper puro `eligibleRoster`/`roleReceivesTips`, null-safe: flag null/ausente → **recibe**) — también del **picker de cobertura**, para que no entre por ningún camino. **Turnos abiertos/cerrados NO se re-tocan**: quien ya tiene entrada se preserva (`keepIds`). **La matemática NO se toca:** `PROPINA_ROLES/NO_PROPINA_ROLES/BAR_ROLES` y `calcTurno` intactos — el rol excluido simplemente no genera línea, así que nunca recibe puntos. Gates verdes: **build prod EXIT 0 · diff de sagrados vs `main` VACÍO** · suite verde salvo el flake TZ conocido de `CashMovimientos.buscarNull`. **(3) EL FLIP DEL MANAGER NO ES UN PENDIENTE TÉCNICO:** es una **decisión del owner** que se controla desde el toggle **"Recibe propina"** en **Admin → Puntos por rol** — **sin deploy, sin SQL, reversible en un clic** (apagarlo lo saca del roster y del pool; prenderlo lo reincorpora). **Estado real al cierre de la sesión: en PROD el manager está en "Sí" (sigue cobrando);** en **staging** quedó en **"No"**. La columna `recibe_propina` y el frontend están **live en prod** (`main` `388a121`), así que el toggle ya opera. **Rollback:** revert de los 2 commits + redeploy · `update role_tip_points set recibe_propina=true where role='manager'` (o el toggle en Admin) · la columna puede quedar (aditiva e inofensiva) o `drop column` si se quiere limpiar. Detalle → HANDOFF-ELEGIBILIDAD-PROPINA.md.
>
> **🆕 2026-07-20 — PASE CORTO A PROD: Estadísticas de propinas → "Distribución por puesto" (validado en staging).** Vehículo: rama **`prod/pase-tipstats-por-puesto`** sobre `main` (`9d55c9c`) — cherry-pick **limpio** de `6c63d16` (**cero conflictos**). **FRONTEND PURO: sin base, sin migración, sin plata** (4 archivos: `TipStats.tsx` + helper + 2 tests). Qué agrega: en **Propinas → Estadísticas, vista general** (sin empleado seleccionado), junto a "Distribución AM vs PM", una **barra por ROL** con su **% del take-home total del mes** (1 decimal) y el monto — **cocina incluida**, es un puesto más. Lógica pura y testeada en **`distribucionPorPuesto`** (agrupa `payout_crc` por rol, `% = rol/total`, orden desc, null-safe, esconde roles con take-home 0; mes sin datos → la sección no se renderiza). **NO se reconcilia con TipCocina ni se doble-cuenta:** el `total` de cocina en `earners` **ya es su tajada real del pool** (points-based) — el reparto igualitario semanal interno de cocina no cambia el total del rol. Colores por puesto consistentes con la paleta del panel; etiquetas vía `ROLE_LABELS`. Gates verdes: **build prod EXIT 0 · diff de sagrados vs `main` VACÍO · suite verde** (367 tests; el flake TZ de `CashMovimientos.buscarNull` no se disparó a esta hora). **`main` código pasa a este pase** + deploy GitHub Pages. Rollback: **revert del commit** + redeploy (sin migración → trivial).
>
> **🆕 2026-07-20 (2º pase del día) — PASE A PROD: 4 mejoras de Caja/Cierre (validadas por el dueño en staging `7beabee` + en piso).** Vehículo: rama **`prod/pase-mejoras-dinamicas`** sobre `main` (`8b21322`) — cherry-pick **limpio** de 5 commits (`bdd48e2` → `a64f529` → `f4a03eb` → `0146de8` → `7beabee`; **cero conflictos**, no tocan ESTADO/PROMPT). **CÓDIGO PURO: cero migraciones · cero esquema · sagrados intactos** (9 archivos, 5 de ellos tests). **Prueba de equivalencia:** los **9 archivos son byte-idénticos a `7beabee`**, o sea a lo que el dueño validó en staging. Qué entra: **(1) Caja Diaria — el asistente de facturas se llama por lo que hace:** el botón verde y el título del modal dicen ambos **"Agregar factura / movimiento"** (antes "Agregar (asistente)" / "Agregar movimiento"). **(2) Pendientes — las propinas dejan de ser proveedores fantasma:** los movimientos `subcategory='Propinas por turno'` caen en **UN solo grupo "Propinas" (🎁)** con **cada turno como fila**, en vez de abrir un grupo por turno vía `description`; el contador de arriba **no cuenta ese grupo como proveedor**; las acciones (pagar/rechazar/comprobante/marcar todos) **siguen operando por `id` de fila, sin cambio de comportamiento**; el comprobante PNG de ese grupo dice **"Comprobante de pago de propinas"** (solo el título dibujado — montos, filas y layout idénticos). **(3) Las secciones de propinas arrancan plegadas:** "Propinas por pagar" (turno) y "Propinas del día" (cierre) se abren a pedido, con **cantidad y total en el encabezado**; estado en **memoria (`useState`), sin `localStorage`/`sessionStorage`**; en el cierre la sección además pasa **DEBAJO de los campos de venta de la fase en curso** (Fase 1 si el mediodía no está sellado, Fase 2 si ya lo está — ramas excluyentes, se renderiza una sola vez). **(4) La Fase 1 se sella con la Caja Diaria ABIERTA:** al mediodía la caja sigue operando y sellar el Mediodía no toca conteo físico ni Caja Fuerte, así que se removió el guard `turnoAbierto` de `handleConfirmParcial` **y del `disabled` del botón** (sin lo segundo el guard removido era letra muerta); el banner pasó de ⚠ bloqueo total a ℹ que aclara que **solo frena la Noche**. **La Fase 2 queda INTACTA:** guard `turnoAbierto`, guard `cajaProvCerrada` y orden de fases (`!parcial`) sin tocar, igual que el gate de ventas-en-0 (`puedeCerrarVentas`/`ventasGateEstado`). **(5) Una propina dejada PENDIENTE se paga por BANCO desde Pendientes:** al saldarla se escribe `{status:'aprobado', method:'Transferencia', caja_origen:'Banco'}` (la vía se decide por el `subcategory` del movimiento real, **no** por el agrupamiento visual) y **`propinasPagadasEnFecha` excluye `method='Transferencia'`** → esa propina **no resta del efectivo esperado del cierre**, porque el turno ya había cerrado y ese efectivo nunca quedó apartado en la caja. El filtro es **`!== 'Transferencia'` y no `=== 'Efectivo'` a propósito**: las filas históricas **sin `method` siguen contando como hasta hoy** (backward-compatible). **"Pagar ahora" queda INTACTO** (`propinaEgresoFields` = **Efectivo/Registradora**) en las dos puertas (CashTurno y CashCierre), y **`tipCalculations` no se toca**. **Efecto colateral verificado y buscado:** `otrosEgresosEf` (CashTurno) filtra `method='Efectivo'`, así que la propina saldada por banco **tampoco resta del efectivo esperado de la Caja Diaria** del turno — coherente con que la plata no salió en efectivo. **Riesgos revisados antes de tocar** (el pase mueve `caja_origen` de una fila existente): el trigger **`cash_movements_unif` (mig 042) se dispara por `classification`/`status`/`account_id`, NO lee `method`/`caja_origen`** (y ya se disparaba con el `updateMovementStatus` previo) · **`saldoCajaFuerte` solo cuenta `caja_origen='Caja Fuerte'`** · **`reconcilePropinaEgreso` machea por `subcategory`+`description`** → ninguno se ve afectado. **Hallazgo de integración (por eso entra `7beabee`):** el caso "mixto" del test de ruteo se rompe al juntar (2) con (5) — ponía una propina dentro del grupo visual de un proveedor, y con la agrupación caen en grupos separados, así que "Marcar todos pagados" (que es **por grupo**) aparece dos veces; además `pagar()` levanta `saving` y deshabilita el resto de los botones, así que dos clicks en el mismo tick perdían el segundo. `7beabee` lo adapta **solo en el test**, conservando lo que fija (el ruteo sale del `subcategory` de la fila). Gates verdes: **`VITE_APP_ENV=production npm run build` EXIT 0** · **303 tests, 301 verdes** — los 2 rojos son el **flake TZ conocido de `CashMovimientos.buscarNull`** (preexistente, `new Date` UTC vs `todayCR`, ajeno a este pase) · **sagrados byte-idénticos a `8b21322` verificado por hash de blob**, no solo por diff: `cashUtils.ts` (`b597c69`) y `tipCalculations.ts` (`7603ba5`). ⚠️ **Ojo con la lista de sagrados:** **`posFiscal.ts` y `computeTotals` NO existen como archivo en `main`** (posFiscal sí existe en `staging`) — chequearlos acá pasa en **vacío**; los sagrados reales de esta rama son los dos de arriba. **`main` código pasa a este pase (`dcc6080` + esta entrada de docs).** Rollback: **revert de los 5 commits** + redeploy (sin migración → trivial). ⏳ **Falta el smoke físico del dueño en PROD.**
>
> **🆕 2026-07-21 — PASE CORTO A PROD: una sola vía para aprobar propinas pendientes (validado en staging `e597206`).** Vehículo: rama **`prod/pase-propina-una-via`** sobre `main` (`f726af1`) — cherry-pick **limpio** de `6b97a46` (**cero conflictos**). **SOLO CÓDIGO: cero migraciones · cero esquema · sagrados intactos** (6 archivos, 3 de ellos tests). Los 6 son **byte-idénticos a `e597206`**, lo que el dueño probó en staging. **Cierra la ÚLTIMA puerta del "ajuste fantasma ≈ propinas".** El pase del 20 tapó la pestaña Pendientes, pero el **select de ESTADO por fila en Movimientos** seguía aprobando la propina **en efectivo**, y eso reabre el descuadre: **`propinasPagadasEnFecha` atribuye el pago a la fecha de la SESIÓN del movimiento** — la del día en que se dejó pendiente, que ya está sellado —, así que la salida **no resta en el "debería" de NINGÚN día** y el cierre queda con un **faltante ≈ el monto de la propina**. **Regla del dueño:** una propina pendiente que se salda, se salda **por BANCO, desde cualquier puerta**. Qué cambia: **(a)** helper puro **`aprobacionPropinaFields()`** en `propinaPago.ts` → `{status:'aprobado', method:'Transferencia', caja_origen:'Banco'}`, con el porqué documentado al lado; **(b)** `CashPendientes.pagar()` pasa a usar el helper en vez del objeto literal — **cero cambio de conducta**; **(c)** `CashMovimientos.handleFieldChange()` recibe la fila y, si es `subcategory='Propinas por turno'` y el cambio es `status → 'aprobado'`, aplica **los 3 campos** en vez de solo el status. **Pasar a 'pendiente' y las filas NO-propina siguen exactamente como antes.** De paso se retiró un bloque `if (field === 'status') {}` vacío. **INTACTOS:** `propinaEgresoFields` ("Pagar ahora" = **Efectivo/Registradora** — ese sí saca efectivo, pero en el día correcto, así que resta donde debe), `propinasPagadasEnFecha`, la math del cierre (**`CashCierre.tsx` con diff VACÍO**) y los sagrados. Gates verdes: **`VITE_APP_ENV=production npm run build` EXIT 0** · **suite 309/309 verde entera** (el flake TZ de `CashMovimientos.buscarNull` no se disparó a esta hora) · **`cashUtils.ts` y `tipCalculations.ts` byte-idénticos a `f726af1`, verificado por hash de blob** · **`git diff -- supabase/` VACÍO**. Tests: 3 casos nuevos en `CashMovimientos.aprobarPropina.test.tsx` (propina → los 3 campos · proveedor → solo status · volver a pendiente → solo status), **verificado que el primero FALLA contra el código de prod** (llegaba `{status:'aprobado'}` pelado), + 3 casos en `propinaPago.test.ts` que documentan la **identidad del neteo negativo** (ventas en efectivo menores a la propina pagada → neto negativo **sin clamp ni valor absoluto**; la misma propina por banco no netea; la suma es aditiva). **➕ Arregla un defecto propio ya presente en `main`:** el test `CashCierre.fase1TurnoAbierto` hardcodeaba `session_date: '2026-07-20'` contra un componente que arranca en `todayStr()` → **empezó a fallar al cambiar el día** (no era flake, quedaba roto de forma permanente); ahora compara contra `todayStr()` y no caduca. **⚠️ El parche NO corrige hacia atrás:** las propinas ya aprobadas en efectivo antes del fix quedan como están — si algún cierre viejo arrastra el faltante fantasma, hay que mirarlo aparte. **⚠️ Alcance:** el helper cierra las **dos** puertas conocidas; si mañana aparece un tercer camino que apruebe movimientos (bulk, import), hay que llamarlo — la regla vive en `propinaPago.ts`, **no hay nada en la base que la imponga**. **`main` código pasa a este pase (`244682a` + esta entrada de docs).** Rollback: **revert del commit** + redeploy (sin migración → trivial). ⏳ **Falta el smoke físico del dueño en PROD.**
>
> Historia detallada del pase → [ESTADO-ARCHIVO.md](ESTADO-ARCHIVO.md) (bloque 2026-07-04) · Fases → [ROADMAP.md](ROADMAP.md) · Backlog / próxima sesión → [PROMPT-CONTINUACION.md](PROMPT-CONTINUACION.md) · Hallazgos → [HALLAZGOS.md](HALLAZGOS.md) · Índice de SPECs → [docs/README.md](docs/README.md).

**Stack:** React 19 + TS strict + Vite + PWA · Supabase (Postgres + RLS + Edge Functions) · realtime.
**Despliegue:** `main` → PROD (GitHub Pages, base `/satori-app/`) · `staging` → Cloudflare Pages (`satori-staging.pages.dev`).

---

## (a) Ramas y proyectos Supabase

| Rama | Hash | Qué es |
|---|---|---|
| `main` | `6c65f25` | **PROD (estable, en uso).** Recibió el **PASE ÚNICO** de la ola 2026-07 (Caja/Cierre/USD/Revisión/asistente + Bandeja + unificación F41–F43, **SIN PoS**) y **🆕 el pase 2026-07-09/10** (propinas efectivo/electrónico + cierre ventas-0/resumen, validado en piso en prod). Ya tenía Olas 1/1.1, pantalla negra, `createDayMovement`, IDOR `extract-document`, outbox `SIGNED_IN`, render Propinas, Actions Node 24. Migs en el ledger **≤021** + **038–046 + subset core de 026, out-of-band**. |
| `staging` | `1daef0c` | **Fuente de verdad del desarrollo.** Todo lo de `main` **+ el PoS/KDS/comandero + FE (SIM) + inventario activo COGS** (migs 022–037). Eso es hoy lo único que separa staging de prod. Migs **022–045** (039–045 out-of-band). |

> **Supabase refs:** **PROD = `yiczgdtirrkdvohdquzf`** ("satori-app") · **STAGING = `hwiatgicyyqyezqwldia`** ("satori-staging").
> 🛑 **RITUAL antes de CUALQUIER comando de base:** `cat supabase/.temp/project-ref` → confirmar el proyecto correcto (NO existe `linked-project.json` en el CLI v2.105). 🆕 **`db query --linked` CUELGA en algunos entornos/sandboxes** (visto 2026-07-04, prod y staging) → workaround: **curl directo a la Management API** (`POST https://api.supabase.com/v1/projects/<ref>/database/query`, token del Keychain macOS, servicio `Supabase CLI`). Ver HALLAZGOS.

## (b) PROD vs STAGING — 🆕 el gran cambio: la unificación ENTERA está en PROD

- **En PROD (`main`, código `92c0831`) — ✅ VALIDADO FÍSICAMENTE por el dueño (smoke 2026-07-06):** ventas/analítica, propinas, caja (turnos + cierre 2 fases + movimientos + pendientes), finanzas/P&L, reportes+emails, admin, auth Fase 2, realtime, offline · estabilidad (Olas 1/1.1, pantalla negra, `createDayMovement`, IDOR, outbox `SIGNED_IN`, render Propinas) · **🆕 Bandeja unificada + unificación Bandeja↔Caja** (asistente "➕ Agregar", Revisión de inventario) · **🆕 toda la ola de cierre/USD:** cierre visual + tema claro + fórmula USD firmada, autorización SOLO por contraseña (mig 045), Tier 3 Revisión/asistente, Opción B (ajuste al ledger), **propinas por la vía real** (faltante fantasma enterrado). Todo el flujo core probado en piso sin errores.
- **Solo en STAGING (NO en prod):** **el PoS completo** (catálogo/salón, comandero, KDS, cobro+splits+ticket SIM, FE estructura SIM, inventario activo COGS) — migs 022–037. **Es lo único que queda por pasar**, y es un proyecto aparte, **DIFERIDO** (bloqueado por el pilar de auth, §f-5).
- **En rama aparte (sin merge):** `propina-pool` (espera decisión). La rama `prod/pase-ola-2026-07` (vehículo del pase) quedó mergeada a `main` por FF y sigue viva en origin.

> ℹ️ **Nota `version.json`:** tras un commit **docs-only** el bundle no cambia; `version.json` en el dispositivo puede seguir mostrando un **hash viejo** hasta que el Service Worker del PWA recicle. No es un fallo de deploy — el sitio se re-buildeó igual.

## (c) Migraciones — 🆕 ahora AMBOS entornos tienen deuda out-of-band

| Entorno | En el ledger (`schema_migrations`) | Aplicadas FUERA del ledger | Notas |
|---|---|---|---|
| **PROD** | **≤021** | **🆕 038–046 + subset core de la 026** (Management API curl; verificadas por privilegio) | 022–037 (PoS) NO están. El pase aplicó 038–045; el **hotfix 2026-07-06** sumó las **secciones 1–4 de la mig 026** (rol `proveedor`, columna `cash_movements.attachments`, bucket `facturas` + 7 políticas); la **046** (`tip_sessions.pool_barra_electronico_crc`, aditiva) entró en el pase **2026-07-10** (+ `NOTIFY pgrst`). Todo aplicó limpio. |
| **STAGING** | **022–038** | **039–045** | 039 dashboard · 040–044 `db query` · 045 `db query --linked`. |

**⚠️ Reconciliación del ledger = sesión dedicada (NO tocar el historial):** ahora **los dos entornos** arrastran migraciones out-of-band (prod suma **038–046 + subset core de 026**; staging **039–046**). Persisten 009 (drift) y 035 (fantasma, solo en `propina-pool`). **`db push`/`repair` FRENADOS** hasta esa sesión. Todo idempotente. **`schema_migrations` NO se tocó** (ni en el pase ni en el hotfix).

## (d) Build por módulo

Gate del pase: **`VITE_APP_ENV=production npm run build` → EXIT 0** (`tsc -b`, **NO** `tsc --noEmit`, falso verde por `tsconfig` raíz con `files:[]`) + suite completa verde (**192 tests** — bajó de 272 al quedar los tests del PoS fuera con sus fuentes). ⚠️ El check **"Supabase Preview"** es **rojo crónico ajeno**; validan `build`+`deploy` (Pages, prod) y `Cloudflare Pages` (staging).

Leyenda: ✅ en prod, maduro / 🟢 en prod, **smoke físico pendiente** / 🧪 solo staging.

| Módulo | Estado | Dónde |
|---|---|---|
| Ventas · Propinas · Caja+cierre · Finanzas/P&L · Reportes · Admin · Auth · Realtime · Offline | ✅ | prod (`cashUtils`/`tipCalculations`/`computeTotals` SAGRADOS) |
| Estabilidad (Olas 1/1.1 · pantalla negra · `createDayMovement` · IDOR · outbox `SIGNED_IN` · render Propinas) | ✅ | prod |
| Unificación Bandeja↔Caja (asistente "➕ Agregar" + Revisión de inventario) | 🟢 EN PROD | prod + staging |
| Cierre visual + tema claro + **fórmula USD firmada** (`calcDeberiaUSD` con `saldoBase.usd`) | 🟢 EN PROD | prod + staging |
| **Autorización de gerencia SOLO por contraseña** (mig 045) + edición de pagos exige autorización | 🟢 EN PROD | prod (mig 045 aplicada) + staging |
| Tier 3 Revisión (foto/panel/adjuntar) + asistente (orden/flujo guiado) | 🟢 EN PROD | prod + staging |
| **Opción B — Ajuste de cierre al ledger** (idempotente por `client_op_id`) | 🟢 EN PROD | prod + staging |
| **Propinas por la VÍA REAL** (resta propinas pagadas; faltante fantasma enterrado) | 🟢 EN PROD | prod + staging |
| PoS (comandero/KDS/cobro/ticket SIM) · FE SIM · Inventario activo COGS | 🧪 solo staging | staging (migs 022–037) |

## (e) Pendientes de PLATA / humanos / fiscales (siguen vivos)

1. **✅ Sinceramiento USD de Caja Fuerte en PROD — HECHO (2026-07-06).** El ajuste inicial (espejo de los −$2678 de staging) se cargó con el conteo físico USD del día → la fórmula USD firmada ya cuadra en prod desde ese punto.
2. **✅ Smoke físico en PROD — HECHO (2026-07-06).** El dueño validó en piso el flujo real (Caja Diaria, asistente foto+IA Sonnet, borrado con contraseña, Cierre con diferencias USD + gate de ajuste, propinas por la vía real) sobre datos de producción — todo pasó.
3. **🖊️ Foto de comprobante obligatoria al pagar propina** — firmado, **DIFERIDO** (fuera de scope de esta ola; pase siguiente). Toca `pagarPropina`/`propinaPago.ts`.
4. **`propina-pool`** (rama, sin merge) → decisión: propina de tarjeta/SINPE ¿al mismo pool que efectivo o separada?

> **Tier 1 (monto-on-modify desde Revisión) = DESCARTADO por la dueña** (la Revisión NO modifica caja). No reabrir sin nueva firma.

## (f) Deuda técnica / decisiones que siguen vivas (detalle en PROMPT-CONTINUACION)

1. **🔴 Reconciliación del ledger de migraciones — ahora en AMBOS entornos.** Sesión dedicada; resolver 009/035 y las out-of-band (prod **038–046 + subset core de 026**, staging **039–046**). Bloquea `db push`.
2. **✅✅ SALDADO EN PROD 2026-07-16 (smoke del dueño HECHO) — Proveedores: el rojo = deuda real.** ⚠️ **El encuadre viejo de este punto era equivocado:** el **"14" NO eran datos huérfanos** de proveedores inexistentes ni hacía falta FK/limpieza — era **`overdueCount`** (agenda de **ciclo de compra**) con la etiqueta engañosa "pagos pendientes"; el diagnóstico read-only (2026-07-06) probó **FK íntegra, 0 dangling**, y **5** pendientes reales (3 legítimos + **2 huérfanos** `supplier_id` NULL, ₡150.043,52). **Decisión FIRMADA (2026-07-09) y ya construida + pasada a prod** (UI/lógica, sin esquema, sagrados intactos): rojo = `pendCount` (deuda real) · agenda de ciclo = indicador **ámbar** aparte · proveedor **'Puntual'** fuera de la agenda (mata el "14") · **✕ Rechazar** en Pendientes con autorización de gerencia. **✅ SMOKE DEL DUEÑO HECHO (2026-07-16): rechazó los 2 huérfanos desde la UI en prod (cero SQL) → pestaña Pendientes 5→3, validado en piso.** Este punto queda **cerrado**.
    **🆕 SIMPLIFICADO EL MISMO DÍA (2º pase 2026-07-16, OK visual del dueño en staging):** viéndolo en prod, el dueño firmó que Proveedores sea **SOLO la lista**. **Ya NO existen en prod** el badge rojo ni el chip ámbar ni los banners de agenda — el rojo era **duplicado** de la pestaña Pendientes (que conserva su badge y su ✕ Rechazar). **La info vive ahora en la tarjeta de cada proveedor** (deuda, ciclo incluido **'Puntual'**, último/próximo pago). ✅ **Smoke saldado igual (2026-07-16):** el rechazo de los 2 huérfanos se hizo desde la pestaña Pendientes (que conserva su badge y su ✕ Rechazar) → **5→3**; el badge rojo en "5" de Proveedores se había retirado por decisión de producto, así que la confirmación vino por Pendientes. Detalle → HALLAZGOS + PROMPT-CONTINUACION P1 #1.
3. **✅ Tokens de GitHub ROTADOS (2026-07-06)** (`gho_` + PAT classic "Claude CLI"). El ESTADO de `main` arrastraba el drift ("rotar YA") mientras staging ya lo daba por hecho — corregido acá para que ambas ramas coincidan.
4. **🖊️👁️ Hora-CR en bordes de período** — las queries de plata (P&L, `finance.ts`) acotan `created_at` en UTC (+6h vs CR) → un cierre de noche puede caer en el período equivocado. Cambia números → valida la dueña.
5. **🖊️ Decisión Etapa 2 de la Bandeja** (entrada foto-primero 100% dentro de Caja Diaria, hoy diseñada sin código) — construir **solo si** tras usar la Etapa 1 en prod sigue haciendo falta.
6. **🚧 PILAR — arquitectura de sesión/auth escalable y multi-tenant** — **bloquea el GRAN PASE del PoS** a prod (~10 dispositivos concurrentes; hotelería/franquicias).

---

## Sagrados (NUNCA reimplementar sin acuerdo explícito)
`cashUtils` · `tipCalculations` · `computeTotals` (fórmula fiscal) · cierres de caja (la matemática del "debería") · cobro/vuelto/conversión · `posFiscal`.
La ola tocó la *plomería* del cierre con firma, pero `tipCalculations`/`calcTurno`/`saldoCajaFuerte` quedaron byte-idénticos. **Gate 5 del pase:** `tipCalculations.ts` vs prod = **+2 líneas** (etiqueta del rol `proveedor`); `cashUtils.ts`/`computeTotals` **idénticos a staging**.
